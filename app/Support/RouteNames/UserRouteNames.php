<?php

namespace App\Support\RouteNames;

class UserRouteNames extends ResourceRouteNames
{
    const INDEX     = 'dashboard.users.index';
    const CREATE    = 'dashboard.users.create';
    const STORE     = 'dashboard.users.store';
    const SHOW      = 'dashboard.users.show';
    const EDIT      = 'dashboard.users.edit';
    const UPDATE    = 'dashboard.users.update';
    const DESTROY   = 'dashboard.users.destroy';
}
