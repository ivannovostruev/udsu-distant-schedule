<form method="POST"
      action="{{ route($routeName, $id) }}"
      onsubmit="return confirmBeforeRemove()"
      class="me-1">
    @method('DELETE')
    @csrf
    <button type="submit"
            class="btn btn-danger"><i class="bi bi-trash me-1"></i>Удалить</button>
</form>
